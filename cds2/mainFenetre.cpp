#include "mainFenetre.h"


mainFenetre::mainFenetre() : QMainWindow()
{
    setFixedSize(460,300);
    setWindowTitle("Credit Default Swap");













    /*next = new QPushButton("Suivant",this);
    quitter = new QPushButton("Quitter",this);

    next->setGeometry(300,200,60,40);
    quitter->setGeometry(380,200,60,40);
    QObject::connect(quitter, SIGNAL(clicked()), qApp, SLOT(quit()));
*/

}
